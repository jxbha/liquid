apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-mana
  namespace: apps
  labels:
    purpose: data-mana
    database: postgresql
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
