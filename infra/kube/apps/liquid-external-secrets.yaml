apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: liquid-external-secrets
  namespace: argocd
  labels:
    name: liquid-external-secrets
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: default
  source:
    repoURL: http://gitea.jbernh.xyz:3000/jxbha/liquid/
    path: infra/kube/platform/external-secrets/overlays/prod
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: external-secrets
  syncPolicy:
    automated:
      selfHeal: true
      enabled: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
