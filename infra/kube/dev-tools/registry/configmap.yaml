apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-config
  labels:
    purpose: registry
data:
  REGISTRY_HTTP_TLS_CERTIFICATE: /etc/registry/certs/tls.crt
  REGISTRY_HTTP_TLS_KEY: /etc/registry/certs/tls.key
