[workers]
%{ for k,v in workers ~}
${k} ansible_host=${v}
%{ endfor ~}

[controllers]
%{ for k,v in controllers ~}
${k} ansible_host=${v}
%{ endfor ~}

[kube:children]
controllers
workers

[kube:vars]
ansible_user=${user}
ansible_ssh_private_key_file=${ssh_key}
