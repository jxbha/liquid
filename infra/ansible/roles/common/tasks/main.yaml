---
# sudo apt-get install -y apt-transport-https ca-certificates curl
- name: Install base packages
  ansible.builtin.apt:
    pkg:
      - apt-transport-https
      - ca-certificates
      - curl
      - gnupg
